from django.conf.urls import url
from django.contrib import admin

from . import views

urlpatterns = [
	#url(r'^$', "tableau.views.post_list", name='list'),
	#url(r'^$', "tableau.views.banking_list", name='banking'),
	#url(r'^create/$', "tableau.views.post_create", name='create'),
	#url(r'^(?P<slug>[\w-]+)/$', "tableau.views.post_detail", name='detail'),
	#url(r'^(?P<slug>[\w-]+)/edit/$', "tableau.views.post_update", name='update'),
	#url(r'^(?P<slug>[\w-]+)/delete/$', "tableau.views.post_delete", name='delete'),
	#url(r'^posts/$', "<appname>.views.<function_name>"),
]


#urls for slug
#(?P<slug>[\w-]+)